/**
 * El frontend de usuario únicamente reconoce y opera con el rol USER
 * (FR-048 en este módulo).
 *
 * Fuente de verdad: specs/001-user-interactions/data-model.md
 */
export enum RolUsuario {
  USER = 'USER',
}
